version: "3.9"

services:
  frontend:
    build: .
    container_name: vedic-app-frontend
    ports:
      - "3000:80"

  frontend-tests:
    build:
      context: .
      target: build # Use your build stage from Dockerfile
    command: npm test
